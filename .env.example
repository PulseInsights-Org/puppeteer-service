# =============================================================================
# PUPPETEER SERVICE - Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
PORT=3000

# NODE_ENV Options:
#   - development : Enables debug logging, relaxed security, non-headless browser
#   - production  : Optimized for production, headless browser, stricter security
NODE_ENV=production

# CORS origin (optional, defaults to * for all origins)
# CORS_ORIGIN=https://your-domain.com

# =============================================================================
# SUPABASE (REQUIRED)
# =============================================================================
# Screenshots are uploaded directly to Supabase storage
# You need a Supabase project with a storage bucket named 'rfq-artifacts'

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =============================================================================
# PUPPETEER OPTIONS (Optional)
# =============================================================================

# Path to Chrome executable (auto-detected if not set)
# For EC2 Amazon Linux 2:
# CHROME_PATH=/usr/bin/google-chrome-stable
# For Ubuntu:
# CHROME_PATH=/usr/bin/google-chrome
# For Alpine (Docker):
# CHROME_PATH=/usr/bin/chromium-browser

# Log level for Puppeteer service
# Options: error, warn, info, debug
PUPPETEER_LOG_LEVEL=info

# Rate limiting for Puppeteer API
# Window in milliseconds (default: 60000 = 1 minute)
PUPPETEER_RATE_LIMIT_WINDOW_MS=60000
# Max requests per window (default: 10)
PUPPETEER_RATE_LIMIT_MAX_REQUESTS=10
